{
  "name": "griffard-family-site",
  "version": "1.0.0",
  "description": "Family website with directory, announcements, events, and photo gallery using Netlify + Cloudinary",
  "main": "index.html",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build step needed' && exit 0"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "form-data": "^4.0.0"
  }
}
